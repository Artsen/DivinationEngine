# Contributing

Use Python 3.12+, add an Alembic revision for every schema change, and run the complete quality
suite documented in the README. Keep HTTP details in API modules, mechanics in the domain, and
transaction orchestration in services. Tests must inject deterministic randomness; never weaken
production randomness to make a test reproducible. Knowledge contributions need explicit rights
and source provenance and must not be machine-invented.
